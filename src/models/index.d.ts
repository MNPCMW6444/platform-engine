interface RawEEG {
  b: string;
}

interface AttentionScore {
  b: string;
}

interface FrequencyRangeInHz {
  minFrequencyiInHz: number;
  maxFrequencyiInHz: number;
}

export type { RawEEG, AttentionScore };
